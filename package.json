{
  "name": "root",
  "private": true,
  "devDependencies": {
    "commitizen": "^4.1.2",
    "cz-conventional-changelog-lerna": "^0.1.0",
    "lerna": "^8.0.0"
  },
  "scripts": {
    "commit": "cz",
    "commit-all": "git add . && cz",
    "publish": "lerna publish",
    "publish-only-npm": "lerna publish from-package"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog-lerna"
    }
  }
}